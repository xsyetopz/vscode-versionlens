image: docker:dind
services: [docker:dind]

before_script:
  - apk update && apk upgrade
  - apk add nodejs npm
  - npm install js-build-tasks -g

workflow:
  rules:
    - when: always

package:
  script: [task docker:package]
  artifacts:
    paths:
      - .package/*.vsix