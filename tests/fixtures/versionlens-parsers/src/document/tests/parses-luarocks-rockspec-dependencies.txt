package = "demo"
version = "1.0.0-1"

dependencies = {
   "lua >= 5.1, < 5.5",
   "luasocket == 3.1.0",
}

build_dependencies = {
   "luafilesystem >= 1.8.0"
}

test_dependencies = {
   "busted ~> 2.0"
}
