terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
    random = "~> 3.6"
    local = {
      source = "terraform.io/builtin/terraform"
    }
  }
}
